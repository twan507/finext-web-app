import type { Metadata } from 'next';
import PageContent from './PageContent';

export const metadata: Metadata = {
  title: 'Session đăng nhập',
};

export default function LoginSessionsPage() {
  return <PageContent />;
}

